// Area of circle

const CircleAreaPerim = (radius) => {
  console.log(`Area of circle is : ${Math.PI * (radius * radius)}`);
  console.log(`Perimeter of circle is : ${2 * Math.PI * radius}`);
};

CircleAreaPerim(10);
